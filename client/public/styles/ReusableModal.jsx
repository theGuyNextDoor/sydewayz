import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  wrapper: {
    justifyContent: 'center',
    height: '100%',
    padding: '5%',

    backgroundColor: '#02003399',
  },
  container: {
    justifyContent: 'center',
    alignItems: 'center',
    height: '60%',
    backgroundColor: '#26C8D2',
    borderRadius: 10,
  },
});

export default styles;
